# 22 — THE FLIP-PACKET BRIEF (Fable, 2026-07-19 — binding)

**Who runs this: Opus, main tree, after reading CLAUDE.md.** The situation this brief exists for:
**§1.4.1 has PASSED (3/3 from the record)** and build-order 6 is complete, so the constraint is no
longer build — it is the pile of RESERVED decisions accumulated across Sessions 12–24. Michael cannot
launch by spelunking canon. This session assembles the decision packet (docs/21 §2: Opus drafts,
Michael rules), then spends the remainder on runnable finding work.

## §0 Session start (Art. XVI)

`git pull` (pull-rebase before every push; expect data-cron commits mid-session). Streak from the
RECORD (`ops.unattended_streak` passes:True is the launch-flip evidence — confirm it still reads
true and say so). `python tests/run_tests.py` — **348+ green** (bare `python` is a stub;
`C:\ProgramData\miniconda3\python.exe`). `vtask list` — reuse, never re-file.

## §1 THE FLIP PACKET (the deliverable: `docs/23-FLIP-PACKET.md`, headed "DRAFT — Michael to ratify")

One document, one sitting for Michael. **Sweep exhaustively** — CLAUDE.md You-are-here, BUILDLOG
Sessions 12–24, the FEATURES registry, the open vtasks — for every reserved decision; the seed list
below is known-incomplete and the sweep must prove its own completeness (grep for "RESERVED",
"Michael", "flip", "NOT self-authorized"). For EACH item: what it is · current state · the evidence
pointer · the options with costs · a recommendation. Group into exactly three tiers:

- **Tier 1 — LAUNCH ACTS (blocking, ordered):** real app passwords (#131) → `POSTING_ENABLED` →
  repo public → announce (#132, gameplan §9 sequence); the #160 data-history ruling (pre-public);
  the #145 GitHub server-side purge residual; #129 receipts-audit + #110 attorney status (report
  state, not pressure); the five docs/16 §9 rulings — mark which actually gate LAUNCH vs which gate
  a FEATURE flip (recommendation: most gate features, not launch — verify per ruling).
- **Tier 2 — FEATURE FLIPS (each an independent one-commit release, NONE launch-blocking):**
  `nomenclature_tags` · `party_columns` · `authors_vessels` · `concordance` (+ PEAK_FLOOR 15,
  MIN_STATEMENTS 10, the two flagged metric definitions) · `awards` (+ MIN_ACTIVE 20, WINDOW 7,
  TOP_N, the thin-day framing question, Void-before-absence-map) · `owners_brief` (#158) · the v2
  shelf flags (archive, silence detector, duet). **Fable's standing steer, Michael free to
  override: launch MINIMAL — Tier 1 only, plus at most `party_columns` + `nomenclature_tags` (both
  are correctness/transparency-adjacent). Every other flip is a scheduled content moment on the
  docs/20 calendar, not a launch-day act.** A dozen simultaneous flips waste a dozen release posts
  and multiply day-one review surface.
- **Tier 3 — PUBLICATION ACTS (post-launch, calendar-paced):** the Intensification cards (#174
  conditions fully met — rider passed and sharpened), the S1.9 launch essay, the S2.9 Boogeyman
  piece; each per docs/20.

File ONE vtask pointing Michael at the packet. The packet states plainly at top: the §1.4.1
evidence, what launches with Tier 1 alone (the site as-is + posting), and that nothing in Tier 2/3
needs deciding on launch day.

## §2 While the packet pends — runnable finding work (fill the session, in this order)

1. **S1.12 Leadership Ignites** — the false block: `leadership_roles` is on disk (156 dated rows,
   congresses 113-119). ~30-min parse + the pre-registered docs/12 measurement, within-lane per
   docs/17 §2 halves, L4 floors as numerals BEFORE measuring, ledger row with script path.
2. **S3.7 Safe-Seat Vessels** — both MEDSL datasets are CC0, no ToS (verified 2026-07-18 via the
   Dataverse API): House `doi:10.7910/DVN/IG0UN2`, Senate `doi:10.7910/DVN/PEJ5QU`. Keyless
   download → offline parse-and-commit `bioguide × cycle → margin` (the committee-names pattern) →
   the pre-registered measurement. ⚠ #143's chamber trap applies to any "vessel" framing — the
   hypothesis's own gates only, no member-leaderboard output.
3. **S5.2** — BILLSTATUS is local/keyless. **BLOCKED on one number**: its floor was never
   pre-registered (the docs/13 p-hacking hole). Draft the floor with a power justification, put it
   in the packet for Michael's one-line confirm, measure only AFTER. Do not self-supply the floor
   and run anyway.
4. Small follow-ups if time (Session-21 list): per-member ingest-health flags in the nightly audit ·
   phrase-page/archive tag surfaces · scaffold-aware key SELECTION (the P2 breadth mitigation —
   a real cluster keeps a better key instead of dying).

## §3 Drip drafts (only if the session still has room)

Draft the two August pieces (S1.9 origin essay, S2.9 Boogeyman) from their cards for Michael's
editing — **to `X:\onscript-data\drafts\`, NEVER in-repo** (the repo goes public at S3; a
pre-publication editorial draft in git history could be quoted as "OnScript said" before Michael
ever edited it). Drafts follow docs/20 §1: receipts, symmetric framing, labels.

## §4 Traps (paid for; do not re-buy)

Foreground tool timeouts are TIMEOUTS. PYTHONHASHSEED=0 near shards. Stage only your own files; a
parallel session may hold the tree — check `git status` before assuming ownership. MEDSL files are
.tab (tab-separated). The awards/concordance builders run inside `deterministic.run` skip-and-log —
a failure there must never crash RUN A (locked tests exist; keep them green).

## §5 Reserved (never self-authorize)

Everything in the packet IS the reserved list — the packet's job is to present it, not decide it.
Also: no posting, no flips, no repo-public, no publication, no S5.2 floor self-supply.

## §6 Session end (Art. XVI)

Suite green · packet committed (DRAFT-marked) + vtask filed · finding rows in docs/13 with
pre-registered floors · BUILDLOG entry · CLAUDE.md You-are-here · expectation-vs-observation
(streak still passing, site day current, flags all off except any Michael has flipped) ·
discrepancies FILED.
